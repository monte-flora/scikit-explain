
2. Check that regression-based models work.
3. Check if a non-sci-kit-learning model works (e.g., XGBoost)
	-> E.g., XGBoost only has the .predict so may want 
	         to check if a model has 'predict_proba'.
			 if model_output == 'probability' 
4. Setting the levels for the 2D plots 



